{
  "name": "unity-transitions",
  "version": "0.0.1",
  "description": "Transitions are behaviours that start and then run to completion with an option to cancel. The most common example is something like a FadeIn for a UI panel. The standalone interfaces package allows other packages to interact with transitions without a dependency on any implementation.",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/beatthat/unity-transitions.git"
  },
  "files": [
    "index.js",
    "scripts",
    "src"
  ],
  "author": "larrykirschner@gmail.com",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/beatthat/unity-transitions/issues"
  },
  "homepage": "https://github.com/beatthat/unity-transitions",
  "scripts": {
    "postinstall": "node scripts/postinstall.js",
    "install:test": "rm -f *.tgz && npm pack && cd test && npm run clean && npm install"
  },
  "dependencies": {
    "mkdirp": "^0.5.1",
    "ncp": "^2.0.0",
    "unity-transition-interfaces": "github:beatthat/unity-transition-interfaces#semver:^0.0.2",
    "unity-requests": "github:beatthat/unity-requests#semver:^0.0.15",
    "unity-extensions": "github:beatthat/unity-extensions#semver:^0.0.3"
  }
}
